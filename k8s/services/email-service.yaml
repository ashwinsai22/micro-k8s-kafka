apiVersion: apps/v1
kind: Deployment
metadata:
  name: email-service
  namespace: microkafka
spec:
  replicas: 1
  selector:
    matchLabels:
      app: email-service
  template:
    metadata:
      labels:
        app: email-service
    spec:
      containers:
        - name: email
          image: ashwinsai22/email-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          env:
            - name: KAFKA_BROKERS
              value: kafka-0.kafka.microkafka.svc.cluster.local:9092
---
apiVersion: v1
kind: Service
metadata:
  name: email-service
  namespace: microkafka
spec:
  selector:
    app: email-service
  ports:
    - port: 80
      targetPort: 3000
